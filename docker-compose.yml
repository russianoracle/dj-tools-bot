version: "3.9"

services:
  bot:
    image: ${REGISTRY:-cr.yandex}/${REGISTRY_ID:-crp78hhr2t67jkeedad1}/mood-classifier:${TAG:-latest}
    container_name: mood-classifier
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - DATA_DIR=/data
      - DOWNLOADS_DIR=/data/downloads
      - MAX_FILE_SIZE_MB=500
    volumes:
      # КРИТИЧНО: /data для БД и кеша (НЕ удаляется при обновлении)
      - mood_data:/data
      - downloads_data:/app/downloads
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - app-network
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 2G
    healthcheck:
      test: ["CMD", "python", "-c", "import redis; r = redis.Redis(host='redis', port=6379); r.ping()"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  worker:
    image: ${REGISTRY:-cr.yandex}/${REGISTRY_ID:-crp78hhr2t67jkeedad1}/mood-classifier:${TAG:-latest}
    container_name: mood-arq-worker
    command: arq app.services.arq_worker.WorkerSettings
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - DATA_DIR=/data
      - DOWNLOADS_DIR=/data/downloads
    volumes:
      # КРИТИЧНО: /data для БД и кеша (НЕ удаляется при обновлении)
      - mood_data:/data
      - downloads_data:/app/downloads
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - app-network
    deploy:
      resources:
        limits:
          memory: 6G
        reservations:
          memory: 3G
    healthcheck:
      test: ["CMD", "python", "-c", "import redis; r = redis.Redis(host='redis', port=6379); r.ping()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  redis:
    image: redis:7-alpine
    container_name: mood-redis
    restart: unless-stopped
    # ВАЖНО: --appendonly yes для персистентности очереди задач
    # При рестарте контейнера ARQ задачи НЕ теряются
    command: redis-server --appendonly yes --maxmemory 2gb --maxmemory-policy allkeys-lru
    volumes:
      # КРИТИЧНО: Redis AOF файлы в volume (задачи не теряются при обновлении)
      - redis_data:/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

volumes:
  # КРИТИЧНО: Персистентные данные (выживают при обновлении контейнеров)
  mood_data:
    # БД (predictions.db), STFT кеш, features кеш
  downloads_data:
    # Временные загрузки пользователей
  redis_data:
    # Redis persistence (AOF) - очередь задач ARQ

networks:
  app-network:
    driver: bridge