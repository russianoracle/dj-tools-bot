# ============================================================================
# PRODUCTION DEPENDENCIES (Docker, Bot Deployment)
# ============================================================================
# Минимальный набор для работы бота и анализа аудио в production
# ~17 пакетов, ~600MB установленных зависимостей (vs ~18GB с full requirements.txt)

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0

# Scientific Computing
numpy>=1.24.0
scipy>=1.10.0

# Machine Learning - ТОЛЬКО инференс (NO tensorflow, NO torch)
scikit-learn>=1.3.0
xgboost>=2.0.0  # Легкий gradient boosting, ~50MB

# Data Processing
pandas>=2.0.0

# Metadata
mutagen>=1.47.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0

# Performance (REMOVED - optional, slow to compile)
# numba>=0.57.0  # JIT compilation для librosa (опционально, +2min build time)

# Telegram Bot
aiogram>=3.0.0
python-dotenv>=1.0.0

# Task Queue
arq>=0.25.0
redis>=5.0.0

# ============================================================================
# OPTIONAL (закомментировано, uncomment если нужно):
# ============================================================================
# essentia-tensorflow>=2.1.0  # Для GenreAnalysisTask (adds ~800MB, TF models)
# torch>=2.0.0                # Для MPS acceleration на macOS (adds ~5GB)
